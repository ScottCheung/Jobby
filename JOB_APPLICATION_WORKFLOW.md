<!-- @format -->

# Jobby 投递工作流（Codex 执行规范）

## 1. 目标与执行边界

- Jobby 负责职位读取、最终得分、Autofill、简历生成与上传。
- Codex 负责调度、补填、纠错、多步骤推进、异常恢复与结果记录。
- 用户只处理登录、验证码、无法确认的敏感问题和最终 Submit。
- 第一版使用 `human_confirmed` 模式：Codex 必须停在最终提交页。

## 2. 启动指令

用户发送以下指令后，Codex 直接执行本文件，不重复确认已配置规则：

```text
执行 Jobby 投递工作流
目标岗位：默认 / 自定义
地区：默认 / 自定义
来源：默认 / 自定义
数量：N
职位链接：可选
```

默认配置：

- 岗位：Full Stack Developer、.NET Developer、Next.js Developer、Frontend Developer。
- 地区：Sydney；接受 Hybrid、Remote、On-site Sydney。
- 每批最多 Tailor：10。

## 3. 数据源优先级

Codex 按以下顺序取值，不要求用户重复提供已有信息：

1. Jobby 核心资料。
2. 当前启用的简历及结构化简历。
3. 已确认的通用、平台和公司答案。
4. 历史申请记录。
5. 当前 JD。
6. 用户确认。

禁止根据猜测补充经历、技能、数字、公司、签证或其他事实。

## 4. 前置检查

每批开始前检查：

- API 和数据库可用。
- Jobby 登录有效。
- 招聘网站登录有效。
- 默认 PDF 可下载。
- Tailor 功能可用。
- 职位未重复申请。

前置检查失败时暂停批次并报告具体问题。

## 5. 评分分流

只使用插件显示的最终得分，不自行重算或覆盖：

- 最终得分 `>= 65`：生成 Tailor 简历后申请。
- 最终得分 `50–64`：使用默认 PDF 申请。
- 最终得分 `< 50`：跳过，并记录批次结果。

默认 PDF 直接使用已保存文件。结构化简历只用于评分和 Tailor，不直接上传。

## 6. 单个职位执行流程

1. 打开职位页并等待页面稳定。
2. 使用插件读取职位和最终得分。
3. 检查职位是否已申请或重复。
4. 按评分选择 Tailor、默认 PDF 或跳过。
5. 进入申请页面并运行 Autofill。
6. 检查所有必填项、错误项和未填项。
7. 使用数据源优先级补填或纠正。
8. 确认正确简历已上传。
9. 在支持的多步骤表单中点击 Next / Continue，重复步骤 5–8。
10. 到达最终 Review / Submit 页面后停止并提醒用户。
11. 用户提交成功后，使用插件或 API 记录为 Applied。

Codex 不得点击最终 Submit。

## 7. Autofill 兜底

插件漏填或错填时：

1. 重新检测当前表单。
2. 读取字段标签、类型、选项和必填状态。
3. 从 Jobby 数据库、简历和历史答案中解析答案。
4. 填入答案并验证页面是否接受。
5. 对失败字段最多重试两次。
6. 无可靠答案时暂停并仅询问该字段。

开放式回答必须基于真实经历和 JD，使用简洁、专业的澳洲职场英语。

用户确认的新答案保存为通用、平台或公司答案，避免重复询问。

## 8. 人工介入

遇到以下情况立即暂停：

- CAPTCHA、Cloudflare 或 MFA。
- 需要用户登录、注册账号或处理密码。
- 数据库没有可靠答案的签证、工作授权、薪资、法律或人口统计问题。
- 页面错误连续两次无法恢复。
- 简历上传失败或最终预览内容异常。
- 最终 Submit。

提醒命令：

```bash
afplay /System/Library/Sounds/Glass.aiff >/dev/null 2>&1 || true
osascript -e 'display notification "申请已准备完成，请审核并提交" with title "Jobby"' >/dev/null 2>&1 || true
```

## 9. 记录规则

- 仅在检测到提交成功后记录为 Applied。
- 保存职位链接、平台、公司、岗位、最终得分和简历类型。
- 跳过职位只写入批次结果，不记录为 Applied。
- 失败职位记录失败步骤和原因。
- 每处理完一个职位立即写入结果，避免重复。

## 10. 批次输出

批次结束后只汇总：

```text
已提交：N
待用户提交：N
默认简历：N
Tailor：N
跳过：N
失败：N
需要补充的信息：...
```

## 11. 第一版完成标准

- 能读取职位并按最终得分分流。
- 能选择默认 PDF 或 Tailor 简历。
- 能完成常规 Autofill 和多步骤推进。
- 能补足大部分插件失败字段。
- 能停在最终提交页提醒用户。
- 能在提交成功后记录申请。
