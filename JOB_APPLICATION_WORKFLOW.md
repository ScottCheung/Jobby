<!-- @format -->

# Jobby 持续投递准备工作流（Codex 执行规范）

## 1. 目标与执行边界

- Jobby 负责职位读取、最终得分、Autofill、简历生成与上传。
- Codex 负责调度、补填、纠错、多步骤推进、异常恢复与结果记录。
- 默认使用 `continuous_prepare` 模式。Codex 可持续筛选、评分、检查表单并准备批次。
- 用户已表达自动提交意愿，但 Codex 的浏览器安全规则仍要求在向具体招聘方传输个人资料及最终 Submit 的实际时点获得确认；提前授权不能替代该确认。
- CAPTCHA、Cloudflare、MFA、登录失效、未知敏感问题或单个页面异常只阻塞当前职位，不得阻塞整批。
- 禁止猜测答案、伪造经历或为了完成数量降低提交标准。
- 无人值守不是绕过招聘网站限制。不得规避 CAPTCHA、MFA、限流或网站明确的自动化限制。

用户已确认的筛选和数量边界：

- 总目标：最多处理 `50` 个岗位。
- 最终得分 `<= 70`：尝试提交。
- 最终得分 `> 70`：不提交，记录到 MD 供用户自行申请。
- 每轮最多准备：`10`。
- 每个自然日最多提交：`100`，按 Australia/Sydney 日期计算。
- 遇到验证码、未知敏感题或异常时跳过并继续，不得猜测答案。

## 2. 启动指令

用户发送以下指令后，Codex 直接执行本文件，不重复确认已配置规则：

```text
执行 Jobby 投递工作流
模式：continuous_prepare
目标岗位：默认 / 自定义
地区：默认 / 自定义
来源：默认 / 自定义
数量：N
职位链接：可选
```

默认配置：

- 岗位：Full Stack Developer、.NET Developer、Next.js Developer、Frontend Developer。
- 地区：Sydney；接受 Hybrid、Remote、On-site Sydney。
- 总目标：50 个岗位。
- 得分 `<= 70`：尝试提交；得分 `> 70`：跳过并在 MD 中记录网址、分数和推荐理由。
- 每轮最多准备：10。
- 每日最多提交：100。
- 通用简历：`/Users/xianzhezhang/jobFinding/src/CV/Scott Zhang_CV.pdf`。
- 初始队列优先读取仓库根目录的 `applied_jobs.md`；队列用完后再读取 Jobby 推荐和已配置职位来源。

## 3. 数据源优先级

Codex 按以下顺序取值，不要求用户重复提供已有信息：

1. Jobby 核心资料。
2. 当前启用的简历及结构化简历。
3. 已确认的通用、平台和公司答案。
4. 历史申请记录。
5. 当前 JD。
6. 用户确认。

禁止根据猜测补充经历、技能、数字、公司、签证或其他事实。

## 4. 前置检查

每批开始前检查：

- API 和数据库可用。
- Jobby 登录有效。
- 招聘网站登录有效。
- 职位未重复申请。
- 当日已提交数量低于 100。

API、数据库、Jobby 登录或浏览器连接等全局依赖不可用时结束本轮并记录原因，等待下次心跳重试。招聘网站登录、单个职位或单个页面失败时只阻塞当前职位并继续下一项。

每轮开始前统计当日已成功提交数量：

- 当日数量已经达到 100 时，本轮不再打开或提交新申请。
- 当日剩余额度少于 10 时，本轮上限等于剩余额度。
- 只有检测到招聘网站提交成功后才计入每日数量。

## 5. 评分分流

只使用插件显示的最终得分，不自行重算或覆盖：

- 最终得分 `<= 70`：使用指定通用 PDF，进入申请流程。
- 最终得分 `> 70`：不申请，在 `applied_jobs.md` 记录网址、分数和插件推荐理由，供用户自行申请。
- 没有可验证的最终得分：跳过并记录 `blocked_score_unavailable`。

不生成 Tailor 简历。评分只使用插件显示的最终得分；通用 PDF 仅在获得操作时确认后上传。

## 6. 单个职位执行流程

1. 打开职位页并等待页面稳定。
2. 使用插件读取职位和最终得分。
3. 检查职位是否已申请或重复。
4. 按评分选择提交或记录到 MD。
5. 进入申请页面，只读检查字段、必填状态和流程，不填写或上传任何个人资料。
6. 对得分 `> 70` 的职位记录网址、分数和推荐理由；对得分 `<= 70` 的职位整理可靠答案并加入最多 10 个的待确认提交批次。
7. 列出该批次的具体招聘方、网站和即将传输的数据，集中请求一次操作时确认。
8. 获得确认后运行 Autofill、补填可靠答案、上传正确简历，并检查所有必填项、错误项和未填项；未获得确认时标记为 `awaiting_data_transmission_confirmation`，不得重复提醒或创建重复批次。
9. 在支持的多步骤表单中点击 Next / Continue，重复填表、上传和验证。
10. 到达最终 Review / Submit 页面后执行提交前硬性检查，并按具体招聘方集中请求最终 Submit 的操作时确认。
11. 获得确认且全部检查通过时点击一次最终 Submit。
12. 等待并检测明确的提交成功页面或成功提示。
13. 仅在确认提交成功后，使用插件或 API 记录为 Applied，并立即持久化结果。

提交前必须同时满足：

- 最终得分不高于 70，岗位和地区符合本轮配置。
- Jobby 与招聘网站的历史记录都没有显示已申请。
- 所有必填字段已完成，页面没有验证错误。
- 每个答案都来自本文件规定的数据源，不含猜测内容。
- 指定通用 PDF 已上传，文件名和最终预览没有异常。
- 页面明确处于当前公司的当前职位申请流程，最终按钮明确表示提交申请。
- 不存在 CAPTCHA、Cloudflare、MFA、未知敏感题或需要接受未确认法律声明的情况。
- 本轮和当日提交数量仍未达到上限。
- 已针对当前具体招聘方和当前批次获得最终 Submit 的操作时确认。

任一条件不满足时不得点击 Submit，应阻塞或跳过当前职位并继续。

## 7. Autofill 兜底

插件漏填或错填时：

1. 重新检测当前表单。
2. 读取字段标签、类型、选项和必填状态。
3. 从 Jobby 数据库、简历和历史答案中解析答案。
4. 填入答案并验证页面是否接受。
5. 对失败字段最多重试两次。
6. 无可靠答案时不得提问等待；将当前职位标记为 `blocked_unknown_answer`，记录字段标签并继续下一职位。

开放式回答必须基于真实经历和 JD，使用简洁、专业的澳洲职场英语。

用户确认的新答案保存为通用、平台或公司答案，避免重复询问。

## 8. 阻塞隔离与自动恢复

遇到以下情况不得等待用户，立即记录当前职位并继续：

- CAPTCHA、Cloudflare 或 MFA。
- 需要用户登录、注册账号或处理密码。
- 数据库没有可靠答案的签证、工作授权、薪资、法律或人口统计问题。
- 页面错误连续两次无法恢复。
- 简历上传失败或最终预览内容异常。
- 招聘网站显示限流、封禁警告或明确禁止继续自动操作。

建议使用以下阻塞或等待原因：

- `blocked_captcha`
- `blocked_login`
- `blocked_unknown_answer`
- `blocked_upload`
- `blocked_page_error`
- `blocked_rate_limit`
- `awaiting_data_transmission_confirmation`
- `awaiting_submit_confirmation`

对同一职位的相同阻塞原因，本轮不重复尝试。下一次心跳只有在阻塞原因可能已经自然恢复时才允许重试；未知答案和法律声明在用户补充可靠答案前不得重试。

## 9. 记录规则

- 仅在检测到提交成功后记录为 Applied；得分 `> 70` 的职位只记录网址、分数和推荐理由，不记录为 Applied。
- 保存职位链接、平台、公司、岗位、最终得分和简历类型。
- 跳过职位只写入批次结果，不记录为 Applied。
- 失败职位记录失败步骤和原因。
- 每处理完一个职位立即写入结果，避免重复。
- 使用平台职位 ID；没有平台 ID 时使用规范化职位 URL 作为幂等键。
- 开始处理时写入 `processing` 和开始时间；超过 30 分钟仍未完成的记录可由下一轮重新领取。
- 成功状态包括明确成功证据，例如 success URL、确认文案或平台申请记录。
- 不得仅因为点击了 Submit 就记录为 Applied。
- `applied_jobs.md` 存在时同步更新对应行状态，保留用户原有内容和格式。

状态流转：

```text
queued -> processing -> submitted
                     -> skipped_low_score
                     -> skipped_duplicate
                     -> blocked_*
                     -> awaiting_*_confirmation
```

## 10. 批次输出

批次结束后只汇总：

```text
已提交：N
通用简历：N
跳过：N
阻塞：N
今日累计已提交：N / 100
阻塞原因：...
```

## 11. 心跳执行规则

- 周期性心跳每次独立执行本文件，单轮最多准备 10 个职位。
- 每轮先恢复超时的 `processing` 项，再领取新的 `queued` 项。
- 单个职位失败后继续处理，不得因为没有用户回复而停留在页面等待。
- 队列暂时为空、达到每日上限或全局依赖不可用时正常结束本轮；后续心跳继续检查。
- 已存在等待确认的批次时，不得重复提醒、重复填表或继续创建新确认批次；等待用户处理后再继续。
- 不得为了修复投递失败而修改 Jobby 源代码。代码缺陷只记录，不在投递心跳中开发或重构。
- 不得开启并行写入同一个招聘网站会话的多个投递任务。

## 12. 持续准备完成标准

- 能读取职位并按最终得分分流。
- 按 `<=70` 提交、`>70` 记录的规则分流，累计最多处理 50 个岗位。
- 能使用指定通用 PDF 完成常规 Autofill 和多步骤推进。
- 能将最多 10 个具体职位整理为可集中确认的批次。
- 能在获得操作时确认且全部硬性检查通过后完成最终提交。
- 能在提交成功后记录申请，并防止重复提交。
- 单个职位阻塞不会停止批次，下一次心跳能恢复未完成工作。
- 始终遵守每轮 10 个、每天 100 个的上限。
