<!-- @format -->

单个文件尽量 ≤ 300 行
一个文件只负责一个主要职责
优先修改现有代码，不要无关重构
除非必要，不要输出完整文件，只输出修改部分
不重复解释已经明确的代码
新功能优先拆成多个小文件，而不是继续堆到一个文件
遇到大文件先重构，再开发新功能
回复控制在例如 500 tokens 以内
只有发现问题才解释原因
不要重复输出已有的 imports / unchanged code
